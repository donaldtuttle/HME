# Legacy provenance

`qosmos_hme_engine_pre_conformance.py` is the unmodified pre-conformance HME source.

```text
SHA-256 6780f974db55380fb4841d3b35c135be10eac8e0c79bc55ff7ff349138febaa6
```

It is retained for byte-level comparison only. Do not import it as the active engine. Its write defaults and QMesh relationship labels assign duties to `Θλ` that the active realization routes through `Σ◯`.
